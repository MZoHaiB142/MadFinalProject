<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="6dp">

    <androidx.constraintlayout.widget.ConstraintLayout
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:id="@+id/card_root"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/bg_country_card"
        android:clickable="true"
        android:focusable="true"
        android:foreground="@drawable/selector_ripple_dark"
        android:minHeight="140dp">

        <!-- Selected Glow Border (shown when selected) -->
        <View
            android:id="@+id/selected_glow"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:background="@drawable/bg_selected_glow"
            android:visibility="gone"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />

        <!-- Top accent bar (colored by region) -->
        <View
            android:id="@+id/top_accent"
            android:layout_width="0dp"
            android:layout_height="3dp"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />

        <!-- Check badge (top-right, shown when selected) -->
        <TextView
            android:id="@+id/tv_check"
            android:layout_width="22dp"
            android:layout_height="22dp"
            android:text="Selected"
            android:textColor="#FFFFFF"
            android:textSize="11sp"
            android:gravity="center"
            android:background="@drawable/bg_check_circle"
            android:visibility="gone"
            android:layout_marginTop="10dp"
            android:layout_marginEnd="10dp"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />

        <!-- Popular badge (optional, shown for top picks) -->
        <TextView
            android:id="@+id/tv_popular_badge"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Popular"
            android:textColor="#FF6B35"
            android:textSize="9sp"
            android:textStyle="bold"
            android:paddingStart="6dp"
            android:paddingEnd="6dp"
            android:paddingTop="2dp"
            android:paddingBottom="2dp"
            android:background="@drawable/bg_popular_badge"
            android:visibility="gone"
            android:layout_marginTop="10dp"
            android:layout_marginStart="10dp"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent" />

        <!-- Flag (large, centered-ish) -->
        <TextView
            android:id="@+id/tv_flag"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Country"
            android:textSize="36sp"
            android:layout_marginTop="28dp"
            android:layout_marginStart="14dp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <!-- Difficulty chip (top-right area) -->
        <TextView
            android:id="@+id/tv_difficulty"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Easy"
            android:textColor="#22C55E"
            android:textSize="9sp"
            android:textStyle="bold"
            android:paddingStart="7dp"
            android:paddingEnd="7dp"
            android:paddingTop="3dp"
            android:paddingBottom="3dp"
            android:background="@drawable/bg_difficulty_chip"
            android:layout_marginTop="28dp"
            android:layout_marginEnd="12dp"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />

        <!-- Country Name -->
        <TextView
            android:id="@+id/tv_country_name"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="Canada"
            android:textColor="#F0F2FF"
            android:textSize="15sp"
            android:textStyle="bold"
            android:letterSpacing="0.01"
            android:layout_marginTop="6dp"
            android:layout_marginStart="14dp"
            android:layout_marginEnd="14dp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toBottomOf="@id/tv_flag" />

        <!-- Timeline info line -->
        <TextView
            android:id="@+id/tv_country_info"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="12-18 months"
            android:textColor="#6B7280"
            android:textSize="11sp"
            android:layout_marginTop="2dp"
            android:layout_marginStart="14dp"
            android:layout_marginEnd="14dp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toBottomOf="@id/tv_country_name" />

        <!-- Divider -->
        <View
            android:id="@+id/card_divider"
            android:layout_width="0dp"
            android:layout_height="1dp"
            android:background="#12FFFFFF"
            android:layout_marginStart="14dp"
            android:layout_marginEnd="14dp"
            android:layout_marginTop="10dp"
            app:layout_constraintTop_toBottomOf="@id/tv_country_info"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />

        <!-- PR / Visa tag -->
        <TextView
            android:id="@+id/tv_pr_tag"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Easy PR"
            android:textColor="#7B93FF"
            android:textSize="10sp"
            android:layout_marginTop="8dp"
            android:layout_marginStart="14dp"
            android:layout_marginBottom="14dp"
            app:layout_constraintTop_toBottomOf="@id/card_divider"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintBottom_toBottomOf="parent" />

        <!-- Steps count -->
        <TextView
            android:id="@+id/tv_steps_count"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="11 steps"
            android:textColor="#4B5563"
            android:textSize="10sp"
            android:layout_marginTop="8dp"
            android:layout_marginEnd="14dp"
            android:layout_marginBottom="14dp"
            app:layout_constraintTop_toBottomOf="@id/card_divider"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toBottomOf="parent" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</FrameLayout>
